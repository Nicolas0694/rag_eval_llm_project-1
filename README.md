Here's a POC of a RAG and it's evaluation system.

The corpus/documents are based on data in PDF and text files. Make sure you installed openai,FAISS, numpy , PyPDF2, ptyhon-dotenv


python -m src pip install openai, faiss-cpu, numpy, PyPDF2, python-dotenv
